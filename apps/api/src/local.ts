/**
 * Base URLs of the local dev servers. Shared by the guest/host apps, the
 * SDK's inline fallbacks, and the Alchemy stack so there is exactly one
 * source of truth for the ports.
 */
export const LocalApiBase = "http://localhost:8787"
export const LocalGuestBase = "http://localhost:5174"
